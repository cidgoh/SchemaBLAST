services:
  solr:
    image: solr:9.2
    container_name: oca_schema_solr
    ports:
      - "8984:8983"
    volumes:
      - solr_data:/var/solr
    environment:
      - SOLR_HEAP=1g
    restart: unless-stopped

volumes:
  solr_data:
